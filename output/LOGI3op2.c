#include <stdio.h>

#include <stdlib.h>

int main() {
    int a,b ,c;

    scanf ("%d", &a);
    scanf ("%d", &b);
    scanf ("%d", &c);

    //Analisa se está entre 0 e 10 
    if(a> 0 && a <10 ){
        printf("Esta entre 0 e 10");
    }
    if(b> 0 && b <10 ){
        printf("Esta entre 0 e 10");
    }
    if(c> 0 && c <10 ){
        printf("Esta entre 0 e 10");
    }

    return 0;
}
     